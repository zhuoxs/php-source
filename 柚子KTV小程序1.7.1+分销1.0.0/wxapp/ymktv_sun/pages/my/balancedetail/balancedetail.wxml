<view class="container">
    <view class="list" wx:for="{{FineBalance}}">
        <view class="flex-1-1">
            <view class="font-30">{{item.details_name}}</view>
            <view class="font-28 color_999999">{{item.addtime}}</view>
        </view>
        <view class="flex-0-0 font-30">{{item.details_money}}</view>
    </view>
</view>
