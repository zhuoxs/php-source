<view class="list" wx:for="{{balancedetail}}">
    <view class="flex-1-1">
        <view class="font-30">{{item.selfmome}}</view>
        <view class="font-28 color_999999">{{item.selftime}}</view>
    </view>
    <view class="flex-0-0 font-30">{{item.selmoney}}</view>
</view>
