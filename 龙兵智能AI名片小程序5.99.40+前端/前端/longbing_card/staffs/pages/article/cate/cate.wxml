<view class="cate-list fill-base">
    <view catchtap="toCheck" class="cate-child b-1px-b f-title ellipsis {{id==item.id?'active':'c-black'}}" data-id="{{item.id}}" data-title="{{item.title}}" wx:for="{{nav}}" wx:key="key">{{item.title}}</view>
</view>
