<template name="wxml60">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml61" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml61" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml61" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml61" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml61" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml61" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml59">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml60" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml60" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml60" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml60" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml60" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml60" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml58">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml59" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml59" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml59" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml59" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml59" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml59" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml57">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml58" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml58" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml58" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml58" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml58" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml58" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml56">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml57" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml57" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml57" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml57" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml57" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml57" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml55">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml56" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml56" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml56" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml56" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml56" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml56" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml54">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml55" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml55" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml55" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml55" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml55" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml55" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml53">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml54" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml54" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml54" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml54" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml54" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml54" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml52">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml53" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml53" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml53" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml53" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml53" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml53" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml51">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml52" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml52" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml52" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml52" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml52" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml52" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml50">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml51" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml51" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml51" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml51" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml51" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml51" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml49">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml28" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml28" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml48">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml49" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml49" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml49" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml49" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml49" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml49" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml47">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml48" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml48" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml48" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml48" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml48" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml48" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml46">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml47" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml47" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml47" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml47" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml47" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml47" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml45">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml46" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml46" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml46" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml46" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml46" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml46" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml44">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml45" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml45" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml45" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml45" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml45" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml45" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml43">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml44" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml44" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml44" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml44" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml44" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml44" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml42">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml43" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml43" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml43" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml43" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml43" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml43" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml41">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml42" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml42" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml42" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml42" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml42" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml42" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml40">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml41" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml41" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml41" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml41" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml41" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml41" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml39">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml40" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml40" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml40" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml40" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml40" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml40" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml38">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml39" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml39" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml39" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml39" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml39" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml39" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml37">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml38" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml38" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml38" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml38" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml38" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml38" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml36">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml37" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml37" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml37" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml37" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml37" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml37" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml35">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml36" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml36" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml36" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml36" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml36" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml36" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml34">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml35" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml35" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml35" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml35" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml35" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml35" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml33">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml34" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml34" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml34" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml34" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml34" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml34" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml32">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml33" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml33" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml33" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml33" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml33" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml33" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml31">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml32" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml32" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml32" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml32" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml32" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml32" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml30">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml31" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml31" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml31" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml31" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml31" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml31" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml29">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml30" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml30" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml30" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml30" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml30" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml30" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml28">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml29" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml29" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml29" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml29" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml29" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml29" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml27">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml28" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml28" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml28" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml26">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml27" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml27" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml27" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml27" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml27" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml27" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml25">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml26" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml26" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml26" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml26" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml26" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml26" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml24">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml25" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml25" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml25" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml25" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml25" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml25" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml23">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml24" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml24" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml24" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml24" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml24" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml24" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml22">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml23" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml23" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml23" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml23" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml23" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml23" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml21">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml22" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml22" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml22" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml22" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml22" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml22" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml20">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml21" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml21" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml21" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml21" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml21" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml21" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml19">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml20" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml20" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml20" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml20" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml20" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml20" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml18">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml19" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml19" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml19" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml19" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml19" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml19" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml17">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml18" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml18" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml18" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml18" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml18" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml18" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml16">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml17" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml17" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml17" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml17" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml17" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml17" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml15">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml16" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml16" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml16" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml16" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml16" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml16" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml14">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml15" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml15" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml15" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml15" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml15" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml15" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml13">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml14" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml14" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml14" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml14" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml14" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml14" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml12">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml13" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml13" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml13" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml13" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml13" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml13" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml11">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml12" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml12" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml12" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml12" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml12" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml12" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml10">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml11" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml11" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml11" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml11" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml11" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml11" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml9">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml10" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml10" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml10" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml10" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml10" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml10" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml8">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml9" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml9" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml9" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml9" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml9" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml9" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml7">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml8" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml8" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml8" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml8" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml8" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml8" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml6">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml7" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml7" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml7" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml7" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml7" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml7" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml5">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml6" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml6" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml6" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml6" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml6" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml6" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml4">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml5" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml5" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml5" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml5" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml5" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml5" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml3">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml4" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml4" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml4" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml4" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml4" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml4" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml2">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml3" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml3" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml3" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml3" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml3" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml3" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml1">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml2" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml2" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml2" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml2" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml2" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml2" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxml0">
    <button size="mini" type="default" wx:if="{{item.tag=='button'}}">
        <template is="wxml1" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </button>
    <view class="{{item.attr.class}} wxml-ul wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ul'}}">
        <template is="wxml1" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'circle'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-ol wxml-block" style="{{item.attr.style}}" wx:elif="{{item.tag=='ol'}}">
        <template is="wxml1" data="{{images:images,item:item,type:item.listtype!=undefined?item.listtype:'number'}}" wx:for="{{item.nodes}}"></template>
    </view>
    <view class="{{item.attr.class}} wxml-li" style="{{item.attr.style}}" wx:elif="{{item.tag=='li'}}">
        <template is="wxmlLiType" data="{{item:item,idx:item.idx,type:type}}"></template>
        <view class="{{item.attr.class}} wxml-li-text">
            <template is="wxml1" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
        </view>
    </view>
    <view bindtap="wxmlTagATap" class="wxml-inline {{item.attr.class}} wxml-a" data-src="{{item.attr.href}}" style="{{item.attr.style}}" wx:elif="{{item.tag=='a'}}">
        <template is="wxml1" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
    <template is="wxmlVideo" data="{{item:item}}" wx:elif="{{item.tag=='video'}}"></template>
    <template is="txvVideo" data="{{item:item}}" wx:elif="{{item.tag=='iframe'}}"></template>
    <template is="wxmlAudio" data="{{item:item}}" wx:elif="{{item.tag=='audio'}}"></template>
    <template is="wxmlImg" data="{{images:images,item:item}}" wx:elif="{{item.tag=='img'}}"></template>
    <text wx:elif="{{item.tag=='#text'}}">{{item.text}}</text>
    <text wx:elif="{{item.tag=='br'}}">
</text>
    <view class="{{item.attr.class}} wxml-{{item.tag}} wxml-{{item.type}}" style="{{item.attr.style}}" wx:else>
        <template is="wxml1" data="{{images:images,item:item}}" wx:for="{{item.nodes}}"></template>
    </view>
</template>
<template name="wxmlLiType">
    <view class="wxml-li-type">
        <view class="{{item.attr.class}} {{type!='number'?'wxml-li-'+type:''}}">
            {{type==='number'?idx+1+'. ':''}}
        </view>
    </view>
</template>
<template name="wxmlImg">
    <image bindload="wxmlImgLoad" bindtap="wxmlImgTap" class="{{item.attr.class}} wxml-img" data-idx="{{item.idx}}" data-src="{{item.attr.src}}" mode="widthFix" src="{{item.attr.src}}" style="max-width:100%;width:{{images[item.idx].width}}px;{{item.attr.style}}"></image>
</template>
<template name="wxmlAudio">
    <view class="{{item.attr.class}} wxml-audio" style="{{item.attr.style}}">
        <audio author="{{item.attr.author}}" class="wx-audio {{item.attr.class}}" controls="{{item.attr.controls!=undefined?true:false}}" loop="{{item.attr.loop!=undefined?true:false}}" name="{{item.attr.name}}" poster="{{item.attr.poster}}" src="{{item.attr.src}}"></audio>
    </view>
</template>
<template name="txvVideo"></template>
<template name="wxmlVideo">
    <view bindtap="wxmlVideoTap" class="{{item.attr.class}} wxml-video" data-src="/longbing_card/users/pages/company/detail/detail?status=toPlayVideo&name=视频播放&src={{item.attr.src}}&shareimg={{item.attr.poster}}" style="position:relative;width:100%;height:400rpx;margin-bottom:0rpx;">
        <image mode="aspectFill" src="{{item.attr.poster||'https://retail.xiaochengxucms.com/images/12/2018/10/Ik4kmm8i4a8Qb5383a699m6p3g3g6q.png'}}" style="width:100%;height:100%;"></image>
        <view class="bg_opacity_sec">
            <image class="image" mode="aspectFill" src="https://retail.xiaochengxucms.com/images/12/2018/10/T8A1maB3boAB3A8Sb8yTYBs1b0BmaA.png"></image>
        </view>
    </view>
</template>
<view class="html2wxml hljs-style-{{highlightStyle}}">
    <block wx:if="{{nodes==undefined}}">
        <view style="padding:20px 0;width:100%;display:flex;justify-content:center;align-items:center" wx:if="{{showLoading}}">
            <image class="loading" src="./images/loading.png"></image>
        </view>
    </block>
    <block wx:else>
        <template is="wxml0" data="{{images:images,item:item}}" wx:for="{{nodes}}"></template>
    </block>
</view>
