<div class="{{viewClass['form-group']}}">

    <label for="{{id}}" class="{{viewClass['label']}} control-label">{{label}}
        {% include 'form/required.twig' %}
    </label>

    <div class="{{viewClass['field']}}">

        {% include 'form/error.twig' %}

        <select class="form-control {{class}}" style="width: 100%;" name="{{name}}[]" multiple="multiple" data-placeholder="{{placeholder}}" {{attributes|raw}} >
            {% for select,option in options %}
                <option value="{{select}}"
                        {% if select == selected[select] %}
                            selected
                        {% endif %}
                >{{option}}</option>
            {% endfor %}
        </select>
        <input type="hidden" name="{{name}}[]" />

        {% include 'form/help-block.twig' %}

    </div>
</div>
