<view>
  
  <view class='allcourse'>
    <view wx:for='{{allarticle}}' wx:key='' bindtap='goWritings' data-id='{{item.id}}' data-goods_id='{{item.goods_id}}'>
      <image src='{{url}}{{item.img}}'></image>
      <view class='allcourse-name '>{{item.title}}</view>
    </view>
  </view>
</view>