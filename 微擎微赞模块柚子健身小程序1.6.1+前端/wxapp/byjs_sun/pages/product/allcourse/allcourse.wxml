<view>
    <view class="allcourse">
        <view bindtap="goBay" data-course_type="{{item.course_type}}" wx:for="{{allcourse}}">
            <view class="dd_mask"></view>
            <image src="{{url}}{{item.type_img}}"></image>
            <view class="allcourse-name ">{{item.course_type}}</view>
        </view>
    </view>
</view>
