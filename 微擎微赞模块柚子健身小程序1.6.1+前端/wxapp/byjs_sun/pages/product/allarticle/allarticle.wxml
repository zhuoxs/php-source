<view>
    <view class="allcourse">
        <view bindtap="goWritings" data-goods_id="{{item.goods_id}}" data-id="{{item.id}}" wx:for="{{allarticle}}">
            <image src="{{url}}{{item.img}}"></image>
            <view class="allcourse-name ">{{item.title}}</view>
        </view>
    </view>
</view>
