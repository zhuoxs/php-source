<view>
    <view bindtap="hide" class="commodity_screen" wx:if="{{codePath}}">
        <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
            <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
        </form>
    </view>
    <view class="hx" wx:if="{{codePath}}">
        <image mode="aspectFit" src="{{url1}}{{codePath}}"></image>
    </view>
    <view class="list">
        <view class="list-img">
            <image src="{{url}}{{product.pic}}" wx:if="{{product.pic}}"></image>
            <image src="../../../resource/images/banner.jpg" wx:if="{{!product.pic&&!cjzt}}"></image>
            <image src="{{url}}{{cjzt}}" wx:if="{{!product.pic&&cjzt}}"></image>
        </view>
        <view class="list-body bg_fff" wx:if="{{product.cid==1}}">
            <view class="list-txt">
                <view class="list-tile flex-1-1 font-30" wx:if="{{product.one1==2}}">奖品：{{product.gname}} X{{product.count}}</view>
                <view class="list-tile flex-1-1 font-30" wx:if="{{product.one==1&&product.one1==1}}">一等奖：{{product.onename}} X{{product.onenum}}</view>
                <view class="list-tile flex-1-1 font-30" wx:if="{{product.one==2&&product.one1==1}}">二等奖：{{product.twoname}} X{{product.twonum}}</view>
                <view class="list-tile flex-1-1 font-30" wx:if="{{product.one==3&&product.one1==1}}">三等奖：{{product.threename}} X{{product.threenum}}</view>
            </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==0}}"> 
        {{product.accurate}} 自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==1}}"> 
        {{product.accurate}} 人自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==2}}"> 
        手动开奖
      </view>
        </view>
        <view class="list-body bg_fff" wx:if="{{product.cid==3}}">
            <view class="list-txt">
                <view class="list-tile flex-1-1 font-30">礼物：{{product.gname}} X{{product.count}}</view>
            </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==0}}"> 
        {{product.accurate}} 自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==1}}"> 
        {{product.accurate}} 人自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==4}}"> 
        直接送礼
      </view>
        </view>
        <view class="list-body bg_fff" wx:if="{{product.cid==4}}">
            <view class="list-txt">
                <view class="list-tile flex-1-1 font-30">皮一下：{{product.gname}} X{{product.count}}</view>
            </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==0}}"> 
        {{product.accurate}} 自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==1}}"> 
        {{product.accurate}} 人自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==4}}"> 
        直接送礼
      </view>
        </view>
        <view class="list-body bg_fff" wx:if="{{product.cid==2}}">
            <view class="list-txt">
                <view class="list-tile flex-1-1 font-34">
                    <text class="color_red font-w">幸运金：</text> 
          {{product.gname}}元 X{{product.count}}
        </view>
                <view class="flex-0-0  danbao color_8d8d8d " wx:if="{{product.status2!=3}}">
                    <view class="danbao-icon">
                        <icon color="#67b34f" size="14" type="success"></icon>
                    </view>
                    <view class="baodan-txt">已担保</view>
                </view>
                <view class="flex-0-0  danbao color_8d8d8d " wx:if="{{product.status2==3}}">
                    <view class="baodan-txt">已过期</view>
                </view>
            </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==0}}"> 
        {{product.accurate}} 自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==1}}"> 
        {{product.accurate}} 人自动开奖
      </view>
            <view class="color_999999 font-26 list-time" wx:if="{{product.condition==2}}"> 
        手动开奖
      </view>
        </view>
        <view class="list2 font-26 bg_fff">
            <view>抽奖发起人</view>
            <view class="list-right">
                <image src="{{url}}{{product.logo}}" wx:if="{{product.logo}}"></image>
                <image src="{{product.img}}" wx:if="{{product.img}}"></image>
                <view>{{product.name}}</view>
            </view>
        </view>
        <view class="txt bg_fff">
            <scroll-view class="txt-container " scrollY="true" wx:if="{{product.status2==4}}">
                <view class="font-30 color_999999 font-w txt-center" style="padding-top:50rpx">很遗憾，你未中奖</view>
                <view bindtap="goTicketmian" class="font-26 jion-num" style="position:relative">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
                    <text class="color_2574b9">去查看更多公共抽奖<text class="font-w">></text>
                    </text>
                </view>
                <block wx:if="{{product.one!=1}}">
                    <view class="title font-28">中奖者名单</view>
                    <view class="list-wrap">
                        <view class="list3" wx:for="{{product.nickName}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                </block>
                <block wx:if="{{product.one==1}}">
                    <view class="title font-28">一等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{ones.length>0}}">
                        <view class="list3" wx:for="{{ones}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{ones.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                    <view class="title font-28">二等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{twos.length>0}}">
                        <view class="list3" wx:for="{{twos}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{twos.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                    <view class="title font-28">三等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{three.length>0}}">
                        <view class="list3" wx:for="{{three}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{three.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{three.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                </block>
            </scroll-view>
            <scroll-view class="txt-container " scrollY="true" wx:if="{{product.status2==3}}">
                <view class="font-30 color_999999 font-w txt-center" style="padding-top:50rpx">很遗憾，该抽奖已失效</view>
                <view bindtap="goTicketmian" class="font-26 jion-num" style="position:relative">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
                    <text class="color_2574b9">去查看更多公共抽奖<text class="font-w">></text>
                    </text>
                </view>
                <view class="title font-28"></view>
            </scroll-view>
            <scroll-view class="txt-container " scrollY="true" wx:if="{{product.cid==1&&product.status2==2||product.cid==3&&product.status2==2||product.cid==3&&product.status2==5||product.cid==3&&product.status2==6||product.cid==1&&product.status2==5||product.cid==1&&product.status2==6||product.cid==4&&product.status2==2}}">
                <view class="gift-wrap">
                    <view class="gift">
                        <text class="iconfont icon-liwu"></text>
                    </view>
                </view>
                <view class="font-30 color_red font-w txt-center">恭喜，你已中奖</view>
                <view class="btn_wrap">
                    <form bindsubmit="goAddress" class="btn1 font-28 color_fff" reportSubmit="true" wx:if="{{!product.adid&&product.cid!=4&&product.status2!=5}}">
                        <button class="btn1 font-28 color_fff" formType="submit">填写收货地址</button>
                    </form>
                    <text style="height:50rpx;width:50rpx;"></text>
                    <button bindtap="verification" class="btn1 font-28 color_fff" data-oid="{{product.oid}}" style="position:relative" wx:if="{{product.cid!=4&&product.status2==2&&product.scanstatus==2}}">
                        <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                            <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                        </form>
            线下核销</button>
                    <button bindtap="goEdAddress" class="btn1 font-28 color_fff" data-oid="{{product.oid}}" wx:if="{{product.adid&&product.status2==6}}">
                        <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                            <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                        </form>
            确认收货</button>
                    <button bindtap="goDiscount" class="btn1 font-28 color_fff" data-oid="{{product.oid}}" wx:if="{{product.cid==3&&product.status2==2&&!product.adid&&product.discount!=0&&product.discount!=null}}">
                        <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                            <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                        </form>
            礼物折现</button>
                </view>
                <block wx:if="{{product.one!=1}}">
                    <view class="title font-28">中奖者名单</view>
                    <view class="list-wrap">
                        <view class="list3" wx:for="{{product.nickName}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                </block>
                <block wx:if="{{product.one==1}}">
                    <view class="title font-28">一等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{ones.length>0}}">
                        <view class="list3" wx:for="{{ones}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{ones.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                    <view class="title font-28">二等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{twos.length>0}}">
                        <view class="list3" wx:for="{{twos}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{twos.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                    <view class="title font-28">三等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{three.length>0}}">
                        <view class="list3" wx:for="{{three}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{three.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                </block>
            </scroll-view>
            <scroll-view class="txt-container" scrollY="true" wx:if="{{product.cid==2&&product.status2==2}}">
                <view class="font-54 color_red font-w txt-center">￥{{product.gname}}</view>
                <view class="font-30 color_red font-w txt-center">恭喜，你已中奖</view>
                <view class="font-26 jion-num">
                    <text class="color_808080">已达到余额，请尽快提现</text>
                    <text bindtap="goBalance" class="color_2574b9" style="position:relative">
                        <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                            <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                        </form>
            去查看<text class="font-w">></text>
                    </text>
                </view>
                <view class="btn_wrap"></view>
                <block wx:if="{{product.one!=1}}">
                    <view class="title font-28">中奖者名单</view>
                    <view class="list-wrap">
                        <view class="list3" wx:for="{{product.nickName}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                </block>
                <block wx:if="{{product.one==1}}">
                    <view class="title font-28">一等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{ones.length>0}}">
                        <view class="list3" wx:for="{{ones}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{ones.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                    <view class="title font-28">二等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{twos.length>0}}">
                        <view class="list3" wx:for="{{twos}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{twos.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                    <view class="title font-28">三等奖中奖者名单</view>
                    <view class="list-wrap" wx:if="{{three.length>0}}">
                        <view class="list3" wx:for="{{three}}">
                            <image class="header" mode="widthFix" src="{{item.img}}"></image>
                            <view class="font-28">{{item.name}}</view>
                        </view>
                    </view>
                    <view class="list-wrap" wx:if="{{three.length<1}}">
                        <view>
                            <text>无</text>
                        </view>
                    </view>
                </block>
            </scroll-view>
        </view>
    </view>
    <view>
        <view bindtap="goTicketnum" class="font-26 jion-num" data-gid="{{product.gid}}">
            <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
            </form>
            <text class="color_808080">已有{{product.allnum}}人参与，</text>
            <text class="color_2574b9">查看全部<text class="font-w">></text>
            </text>
        </view>
        <view class="header-img-box">
            <image class="header-img" mode="widthFix" src="{{item.img}}" wx:for="{{product.imgArr}}"></image>
        </view>
        <button bindtap="onShareAppMessage" class="btn-4 font-30 font-w color_fff" openType="share" style="position:relative" wx:if="{{product.cid==3&&product.status2==2&&!product.adid&&product.state==1}}">
            <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
            </form>
    转赠他人</button>
        <button bindtap="goTicketadd" class="btn-3 font-30 font-w color_fff" style="position:relative">
            <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
            </form>
    发起抽奖</button>
    </view>
</view>
<view class="xinfeng_wrap" wx:if="{{lettershow==true}}">
    <image class="xinfeng-bottom" src="../../../resource/images/before.png"></image>
    <view class="xinfeng-top">
        <image class="img-fanzhuan" src="../../../resource/images/fanzhuan.png"></image>
    </view>
    <view class="xinfeng-after" style="z-index:9">
        <image class="img-top" src="../../../resource/images/fanzhuan.png"></image>
    </view>
    <view class="xin-txt" style="{{txt==true?'opacity: 1;':'opacity: 0;'}}" wx:if="{{txt==true}}">
        <scroll-view class="txt-container " scrollY="true" wx:if="{{product.cid==1}}">
            <icon bindtap="closeletter" class="btn_close" color="#d14c43" type="clear">
                <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                    <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                </form>
            </icon>
            <view class="gift-wrap">
                <view class="gift">
                    <text class="iconfont icon-liwu"></text>
                </view>
            </view>
            <view class="font-30 color_red font-w txt-center">恭喜，你已中奖</view>
            <view class="btn_wrap">
                <button bindtap="goAddress" class="btn1 font-28 color_fff" wx:if="{{!product.adid&&product.cid!=4&&product.status2!=5}}">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
            填写收货地址</button>
                <button bindtap="goEdAddress" class="btn1 font-28 color_fff" data-oid="{{product.oid}}" wx:if="{{product.adid&&product.status2==6&&!product.adid}}">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
            确认收货</button>
                <text style="height:50rpx;width:50rpx;"></text>
                <button bindtap="goDiscount" class="btn1 font-28 color_fff" data-oid="{{product.oid}}" wx:if="{{product.cid==3&&product.status2==2&&!product.adid&&product.discount!=0&&product.discount!=null}}">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
            礼物折现</button>
            </view>
            <block wx:if="{{product.one!=1}}">
                <view class="title font-28">中奖者名单</view>
                <view class="list-wrap">
                    <view class="list3" wx:for="{{product.nickName}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
            </block>
            <block wx:if="{{product.one==1}}">
                <view class="title font-28">一等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{ones.length>0}}">
                    <view class="list3" wx:for="{{ones}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{ones.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">二等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{twos.length>0}}">
                    <view class="list3" wx:for="{{twos}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{twos.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">三等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{three.length>0}}">
                    <view class="list3" wx:for="{{three}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{three.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
            </block>
        </scroll-view>
        <scroll-view class="txt-container " scrollY="true" wx:if="{{product.cid==3}}">
            <icon bindtap="closeletter" class="btn_close" color="#d14c43" type="clear">
                <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                    <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                </form>
            </icon>
            <view class="gift-wrap">
                <view class="gift">
                    <text class="iconfont icon-liwu"></text>
                </view>
            </view>
            <view class="font-30 color_red font-w txt-center">恭喜，你已中奖</view>
            <view class="btn_wrap">
                <button bindtap="goAddress" class="btn1 font-28 color_fff" wx:if="{{!product.adid&&product.cid!=4&&product.status2!=5}}">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
            填写收货地址</button>
                <button bindtap="goEdAddress" class="btn1 font-28 color_fff" data-oid="{{product.oid}}" wx:if="{{product.adid&&product.status2==6}}">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
            确认收货</button>
                <text style="height:50rpx;width:50rpx;"></text>
                <button bindtap="goDiscount" class="btn1 font-28 color_fff" data-oid="{{product.oid}}" wx:if="{{product.cid==3&&product.status2==2&&!product.adid&&product.discount!=0&&product.discount!=null}}">
                    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                    </form>
            礼物折现</button>
            </view>
            <block wx:if="{{product.one!=1}}">
                <view class="title font-28">中奖者名单</view>
                <view class="list-wrap">
                    <view class="list3" wx:for="{{product.nickName}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
            </block>
            <block wx:if="{{product.one==1}}">
                <view class="title font-28">一等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{ones.length>0}}">
                    <view class="list3" wx:for="{{ones}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{ones.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">二等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{twos.length>0}}">
                    <view class="list3" wx:for="{{twos}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{twos.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">三等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{three.length>0}}">
                    <view class="list3" wx:for="{{three}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{three.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
            </block>
        </scroll-view>
        <scroll-view class="txt-container " scrollY="true" wx:if="{{product.cid==2}}">
            <icon bindtap="closeletter" class="btn_close" color="#d14c43" type="clear">
                <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                    <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                </form>
            </icon>
            <view class="font-54 color_red font-w txt-center">￥{{product.gname}}</view>
            <view class="font-30 color_red font-w txt-center">恭喜，你已中奖</view>
            <view bindtap="goBalance" class="font-26 jion-num">
                <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                    <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                </form>
                <text class="color_808080">已达到余额，请尽快提现</text>
                <text class="color_2574b9">去查看<text class="font-w">></text>
                </text>
            </view>
            <view class="btn_wrap"></view>
            <block wx:if="{{product.one!=1}}">
                <view class="title font-28">中奖者名单</view>
                <view class="list-wrap">
                    <view class="list3" wx:for="{{product.nickName}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
            </block>
            <block wx:if="{{product.one==1}}">
                <view class="title font-28">一等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{ones.length>0}}">
                    <view class="list3" wx:for="{{ones}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{ones.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">二等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{twos.length>0}}">
                    <view class="list3" wx:for="{{twos}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{twos.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">三等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{three.length>0}}">
                    <view class="list3" wx:for="{{three}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{three.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
            </block>
        </scroll-view>
        <scroll-view class="txt-container " scrollY="true" wx:if="{{product.cid==4}}">
            <icon bindtap="closeletter" class="btn_close" color="#d14c43" type="clear">
                <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                    <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                </form>
            </icon>
            <view class="font-54 color_red font-w txt-center">{{product.gname}}</view>
            <view class="font-30 color_red font-w txt-center">恭喜，你已中奖</view>
            <view bindtap="goBalance" class="font-26 jion-num">
                <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                    <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
                </form>
            </view>
            <view class="btn_wrap"></view>
            <block wx:if="{{product.one!=1}}">
                <view class="title font-28">中奖者名单</view>
                <view class="list-wrap">
                    <view class="list3" wx:for="{{product.nickName}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
            </block>
            <block wx:if="{{product.one==1}}">
                <view class="title font-28">一等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{ones.length>0}}">
                    <view class="list3" wx:for="{{ones}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{ones.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">二等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{twos.length>0}}">
                    <view class="list3" wx:for="{{twos}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{twos.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
                <view class="title font-28">三等奖中奖者名单</view>
                <view class="list-wrap" wx:if="{{three.length>0}}">
                    <view class="list3" wx:for="{{three}}">
                        <image class="header" mode="widthFix" src="{{item.img}}"></image>
                        <view class="font-28">{{item.name}}</view>
                    </view>
                </view>
                <view class="list-wrap" wx:if="{{three.length<1}}">
                    <view>
                        <text>无</text>
                    </view>
                </view>
            </block>
        </scroll-view>
    </view>
</view>
<view>
    <view bindtap="isLogin" class="isMask" wx:if="{{isLogin}}"></view>
    <view id="isLogin" wx:if="{{isLogin}}">
        <view class="loginTitle">微信授权</view>
        <view class="loginshopImg">
            <span>店铺名称获取以下权限：</span>
        </view>
        <view class="isAgary">
            <text>•</text>
            <view>获取你的公开信息(昵称、头像等)</view>
        </view>
        <view class="isLoginBtn">
            <button bindgetuserinfo="bindGetUserInfo" openType="getUserInfo">授权</button>
        </view>
    </view>
</view>
<image bindtap="gohome" class="fix_honme" src="../../../resource/images/home.png">
    <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
        <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
    </form>
</image>
