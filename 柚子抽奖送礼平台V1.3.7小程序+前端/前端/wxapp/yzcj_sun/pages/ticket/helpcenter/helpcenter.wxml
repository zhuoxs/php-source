<view class="container">
    <view class="help bg_fff" wx:for="{{problem}}">
        <view class="font-28 font-w help-title">{{item.title}}</view>
        <view class="font-26 color_808080">{{item.answer}}</view>
    </view>
</view>
