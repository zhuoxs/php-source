<view class="box1" wx:for="{{msg}}">
    <view class="box1-list">
        <view>{{item.name}}  <text class="color_red">x{{item.num}}</text>
        </view>
        <image src="{{item.imgSrc}}"></image>
    </view>
    <view class="box1-list1">
        <view>抽奖发起热</view>
        <view class="flex">
            <image src="{{userInfo.avatarUrl}}"></image> {{userInfo.nickName}}</view>
    </view>
</view>
<view class="box2_wrap">
    <view class="box2_dot">
        <view class="dot" style="top:{{item.topCircle}}rpx;left:{{item.leftCircle}}rpx;background-color: {{index%2==0?colorCircleFirst:colorCircleSecond}};" wx:for="{{circleList}}"></view>
    </view>
    <view class="box2-3" style="display:{{display}};">
        <view class="box2-3-title1">已开奖</view>
        <view class="box2-3-title2">恭喜以下 <text>{{prizenum}}</text> 为中奖者</view>
        <button class="btn2" wx:if="{{oneself}}">炫耀一下</button>
    </view>
    <view class="box2">
        <view class="box2-con">
            <view class="box2-con-list_wrap">
                <view animation="{{animationData}}" class="box2-con-list">
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                </view>
            </view>
            <view class="box2-con-list_wrap">
                <view animation="{{animationData1}}" class="box2-con-list">
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                </view>
            </view>
            <view class="box2-con-list_wrap">
                <view animation="{{animationData2}}" class="box2-con-list">
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                    <image src="{{item}}" wx:for="{{imgUrls}}"></image>
                </view>
            </view>
        </view>
    </view>
</view>
<view animation="{{animationData3}}" class="box3" style="height:{{height}};">
    <view class="bg_fff">
        <view class="box3-title">中奖者名单</view>
        <view class="box3_con">
            <view class="box3-list">
                <image class="" src="{{userInfo.avatarUrl}}"></image>
                <view>{{userInfo.nickName}}</view>
            </view>
        </view>
    </view>
    <view>
        <view class="box3-title">感谢 <text class="color_blue">1</text> 位参与者</view>
        <view class="box3-list2">
            <image class="" src="{{userInfo.avatarUrl}}"></image>
        </view>
    </view>
    <button class="btn">{{oneself==true?'再发起一个抽奖':'我也要发起抽奖'}}</button>
</view>
