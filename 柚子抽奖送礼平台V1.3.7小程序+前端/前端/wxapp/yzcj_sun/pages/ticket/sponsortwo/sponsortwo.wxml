<view>
    <view class="list font-32 bg_fff">
        <text class="flex-0-0">状态：</text>
        <view class="list-input-1 flex-1-1" wx:if="{{sponsor.status==1}}">审核中</view>
        <view class="list-input-1 flex-1-1" wx:if="{{sponsor.status==2}}">审核通过，已为赞助商</view>
        <view class="list-input-1 flex-1-1" wx:if="{{sponsor.status==3}}">审核失败</view>
        <view class="list-input-1 flex-1-1" wx:if="{{sponsor.status==4}}">赞助结束</view>
        <view class="list-input-1 flex-1-1" wx:if="{{sponsor.status==5}}">申请续费中</view>
    </view>
    <view wx:if="{{sponsor.status==2}}">
        <view class="list font-32 bg_fff">
            <text class="flex-0-0">开始时间：</text>
            <view class="list-input-1 flex-1-1">{{sponsor.time}}</view>
        </view>
        <view class="list font-32 bg_fff">
            <text class="flex-0-0">结束时间：</text>
            <view class="list-input-1 flex-1-1">{{sponsor.endtime}}</view>
        </view>
        <view class="list font-32 bg_fff">
            <text class="flex-0-0">赞助期限：</text>
            <view class="list-input-1 flex-1-1">{{sponsor.day}}天</view>
        </view>
        <view class="list font-32 bg_fff">
            <text class="flex-0-0">公司名称：</text>
            <view class="list-input-1 flex-1-1">{{sponsor.sname}}</view>
        </view>
        <view class="list2 font-32 bg_fff">
            <view class="flex-0-0">公司简介：</view>
            <view class="flex-1-1 newline">{{sponsor.synopsis}}</view>
        </view>
        <view class=" font-32 bg_fff list1">
            <text class="flex-0-0  newline">公司详情：</text>
        </view>
        <view class=" font-32 bg_fff list1">
            <rich-text nodes="{{sponsor.content}}"></rich-text>
        </view>
        <view class="txt-center">
            <image src="{{url}}{{sponsor.ewm_logo}}"></image>
        </view>
    </view>
</view>
<view class="list font-32 bg_fff" wx:if="{{sponsor.status==4}}">
    <view class="list-input-1 flex-1-1">
        <button bindtap="renewal" data-id="{{sponsor.sid}}" style="background:red;color:white;position:relative">
            <form bindsubmit="oddPushFormSubmit" reportSubmit="true" style="width: 100%; height:100%; position: absolute; top: 0; left: 0">
                <button formType="submit" plain="true" style="width: 100%;height:100%;border:1px solid transparent;background-color:transparent;position: absolute; top: 0; left: 0;-webkit-transform: scale(0.5);transform:scale(1);content:\"\";z-index:1000;"></button>
            </form>
      申请续费</button>
    </view>
</view>
<view class="list font-32 bg_fff" wx:if="{{sponsor.length==0}}">
    <view class="flex-0-1">您尚未申请成为赞助商，请于首页最下方申请成为赞助商。</view>
</view>
