<view class="container">
    <view class="head">
        <view class="head_phb">
            <navigator class="head_phb_text" url="/sudu8_page_plugin_sign/ranking/ranking">排行榜</navigator>
            <view class="head_phb_view" style="background-color:{{baseinfo.base_color2}}"></view>
        </view>
        <view class="head_phb">
            <navigator class="head_phb_text" url="/sudu8_page_plugin_sign/new/new">最新签到</navigator>
            <view class="head_phb_view"></view>
        </view>
    </view>
    <view class="topthree" wx:if="{{paixu[0]}}">
        <view class="second">
            <image class="top" mode="widthFix" src="../../sudu8_page/resource/img/t2.png" wx:if="{{paixu[1]}}"></image>
            <image class="avatar" src="{{paixu[1]['avatar']}}" wx:if="{{paixu[1]}}"></image>
            <text class="second_text1" wx:if="{{paixu[1]}}">{{paixu[1]['nickname']}}</text>
            <view class="second_text2" wx:if="{{paixu[1]}}">签到<span>{{paixu[1]['all_count']}}</span>天</view>
        </view>
        <view class="first">
            <image class="top" mode="widthFix" src="../../sudu8_page/resource/img/t1.png" wx:if="{{paixu[0]}}"></image>
            <image class="avatar" src="{{paixu[0]['avatar']}}" wx:if="{{paixu[0]}}"></image>
            <text class="second_text1" wx:if="{{paixu[0]}}">{{paixu[0]['nickname']}}</text>
            <view class="second_text2" wx:if="{{paixu[0]}}">签到<span>{{paixu[0]['all_count']}}</span>天</view>
        </view>
        <view class="three">
            <image class="top" mode="widthFix" src="../../sudu8_page/resource/img/t3.png" wx:if="{{paixu[2]}}"></image>
            <image class="avatar" src="{{paixu[2]['avatar']}}" wx:if="{{paixu[2]}}"></image>
            <text class="second_text1" wx:if="{{paixu[2]}}">{{paixu[2]['nickname']}}</text>
            <view class="second_text2" wx:if="{{paixu[2]}}">签到<span>{{paixu[2]['all_count']}}</span>天</view>
        </view>
    </view>
    <view class="paiming" wx:if="{{paixu[3]}}">
        <view class="num">NO.4</view>
        <image class="paiming_image" src="{{paixu[3]['avatar']}}"></image>
        <text class="paiming_text1">{{paixu[3]['nickname']}}</text>
        <view class="paiming_text2">签到<span>{{paixu[3]['all_count']}}</span>天</view>
    </view>
    <view class="paiming" wx:if="{{paixu[4]}}">
        <view class="num">NO.5</view>
        <image class="paiming_image" src="{{paixu[4]['avatar']}}"></image>
        <text class="paiming_text1">{{paixu[4]['nickname']}}</text>
        <view class="paiming_text2">签到<span>{{paixu[4]['all_count']}}</span>天</view>
    </view>
    <view class="paiming" wx:if="{{paixu[5]}}">
        <view class="num">NO.6</view>
        <image class="paiming_image" src="{{paixu[5]['avatar']}}"></image>
        <text class="paiming_text1">{{paixu[5]['nickname']}}</text>
        <view class="paiming_text2">签到<span>{{paixu[5]['all_count']}}</span>天</view>
    </view>
    <view class="paiming" wx:if="{{paixu[6]}}">
        <view class="num">NO.7</view>
        <image class="paiming_image" src="{{paixu[6]['avatar']}}"></image>
        <text class="paiming_text1">{{paixu[6]['nickname']}}</text>
        <view class="paiming_text2">签到<span>{{paixu[6]['all_count']}}</span>天</view>
    </view>
    <view class="paiming" wx:if="{{paixu[7]}}">
        <view class="num">NO.8</view>
        <image class="paiming_image" src="{{paixu[7]['avatar']}}"></image>
        <text class="paiming_text1">{{paixu[7]['nickname']}}</text>
        <view class="paiming_text2">签到<span>{{paixu[7]['all_count']}}</span>天</view>
    </view>
</view>
