<view class="custom-class utils.bem('tab__pane', { active, inactive: !active }) }}" style="{{animated||active?'':'display: none;'}} {{width?'width:'+width+'px;':''}}" wx:if="{{animated||inited}}">
    <slot></slot>
</view>
<wxs module="utils" src="../wxs/utils.wxs" />