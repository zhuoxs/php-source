<view bind:tap="onClick" class="van-badge van-hairline custom-class {{active?'van-badge--active':''}}">
    <view class="van-badge__text">
        <van-info customStyle="right: 4px" info="{{info}}" wx:if="{{info!==null}}"></van-info>
    {{title}}
  </view>
</view>
