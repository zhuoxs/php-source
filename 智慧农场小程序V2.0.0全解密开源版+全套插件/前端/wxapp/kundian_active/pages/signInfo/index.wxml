<view class="sing_wrapper">
    <view class="sing_item" wx:if="{{item.avatarurl||item.nickname}}" wx:for="{{sign}}" wx:key="id">
        <image src="{{item.avatarurl}}"></image>
        <view>{{item.nickname}}</view>
    </view>
</view>
