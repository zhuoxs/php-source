<view class="container">
    <image class="topImg" mode="widthFix" src="{{imgLink+img}}" wx:if="{{img}}"></image>
    <view class="wall"></view>
    <view bindtap="onRuleDetails" class="serverItem" data-idx="{{index}}" wx:for="{{list}}" wx:key="{{index}}">
        <text class="serverText">{{item.title}}</text>
        <image class="arrow" src="../../../resource/images/wode/jiantou.png"></image>
    </view>
</view>
