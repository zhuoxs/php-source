<view class="container" wx:if="{{nonePage===0}}">
    <image class="imgs" mode="widthFix" src="{{imgLink+msg.pic}}" wx:if="{{msg.pic}}"></image>
    <view class="title">{{msg.title}}</view>
    <view class="time">{{msg.createtime}}</view>
    <view class="details">{{msg.content}}</view>
    <view class="titles">活动详情:</view>
    <view class="details">{{msg.details}}</view>
    <view bindtap="goYuding" class="yuding-btn">立即预订</view>
</view>
<sec-login></sec-login>
