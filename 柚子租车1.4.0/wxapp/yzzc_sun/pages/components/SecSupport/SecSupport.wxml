<view class="zhichi" wx:if="{{msg}}">
    <view class="logob_box">
        <image class="logo_b" src="{{imgLink+msg.sup_logo}}" wx:if="{{msg.sup_logo}}"></image>
    </view>
    <view class="center_word">
        <view class="logo_name">{{msg.sup_name}}</view>
        <view>合作电话:{{msg.sup_tel}}</view>
    </view>
    <view bindtap="_onCallTab" class="callphone">拨打</view>
</view>
