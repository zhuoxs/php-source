<view hidden="{{modalHidden}}" wx:if="{{!isLogin}}">
    <view hidden="{{modalHidden}}">
        <view class="isMask"></view>
        <view class="isLogin">
            <view class="loginTitle">微信授权</view>
            <view class="loginshopImg">
                <view>小程序将获取以下权限：</view>
                <view>(如未授权，可能无法正常使用该小程序)</view>
            </view>
            <view class="isAgary">
                <text>•</text>
                <view>获取你的公开信息(昵称、头像等)</view>
            </view>
            <view class="isLoginBtn">
                <button bindgetuserinfo="_getUserInfo" openType="getUserInfo">授权</button>
            </view>
        </view>
    </view>
</view>
