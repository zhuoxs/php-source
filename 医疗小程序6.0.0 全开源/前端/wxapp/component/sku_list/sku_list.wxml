<view class="bsbb pl30 pr30 h80 bgcf df aic" wx:for="{{keArr}}" wx:key="unique">
    <text bindtap="detailClick" class="ml15 pl15 fs26 c6" data-data="{{item.name}}" data-id="{{item.id}}">{{item.name}}</text>
</view>
