<view class="video">
    <view class="covers" wx:if="{{pause}}">
        <image lazyLoad="true" src="{{poster}}"></image>
        <image class="player" lazyLoad="true" src="/static/images/players1.png"></image>
    </view>
    <video poster="{{poster}}" src="{{src}}" wx:else></video>
</view>
