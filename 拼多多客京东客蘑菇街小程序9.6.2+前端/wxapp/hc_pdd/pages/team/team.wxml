<imperial backgroundColor="{{backgroundColor}}" bind:disappear="listdisappear" id="imperial" title="团队成员"></imperial>
<view class="tuyuaj">
    <view class="whole">
        <text bindtap="qiehuan" class="{{index==chshi?'border':''}}" data-index="{{index}}" wx:if="{{index<fx_level}}" wx:for="{{yiji}}">{{item}}</text>
    </view>
</view>
<view class="huiu"></view>
<view class="caof">
    <view class="tuijiare" wx:for="{{goodslist}}">
        <view class="sanzhong">
            <image class="user_img" src="{{item.head_pic}}"></image>
            <image class="huichan" src="../../resource/images/hui.png"></image>
        </view>
        <view class="zhongbu">
    {{item.nick_name}}
  </view>
        <text class="homa">{{item.juese}}</text>
    </view>
</view>
