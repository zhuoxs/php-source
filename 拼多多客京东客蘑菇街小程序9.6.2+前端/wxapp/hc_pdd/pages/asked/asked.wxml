<view bindtap="dinajf" class="problem" data-index="{{index}}" wx:for="{{wenzi}}">
    <view class="problem_one">
        <text class="zidau">{{item.name}}</text>
        <image class="zidauimg" src="../../resource/images/right_onr.png" wx:if="{{item.checked==0}}"></image>
        <image class="zidauimg" src="../../resource/images/xisa.png" wx:if="{{item.checked!=0}}"></image>
    </view>
    <text class="sycdsaj" data-da="{{item.checked}}" wx:if="{{item.checked!=0}}">
    {{item.daan}}
    </text>
</view>
