<view class="yaqing">
    <image class="qingimg" src="../../resource/images/huo.png" wx:if="{{invite_bg==''}}"></image>
    <image class="qingimg" src="{{invite_bg}}" wx:if="{{invite_bg!=''}}"></image>
    <view class="sjacbA">
        <view class="baohun">
            <view class="gdzltz">
                <text>激活码</text>
                <text>{{activation}}</text>
            </view>
        </view>
        <button bindgetuserinfo="getUserInfo" class="loging" lang="zh_CN" openType="getUserInfo" type="primary">
        立即登录
      </button>
        <text class="cas">提示：激活注册时，请检查激活码是否正确，注册后不可更改</text>
    </view>
</view>
