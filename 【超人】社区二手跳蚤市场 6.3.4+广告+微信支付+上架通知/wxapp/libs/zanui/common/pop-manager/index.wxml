<view class="pop pop--{{type}} {{show?'pop--show':''}}">
    <view bindtap="handleMaskClick" class="pop__mask {{showOverlay?'':'pop__mask--hide'}}" wx:if="{{overlay}}"></view>
    <view class="pop__container">
        <slot></slot>
    </view>
</view>
